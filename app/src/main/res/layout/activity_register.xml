<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/registerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF5F5F5"
    tools:context=".feature.auth.register.RegisterActivity">

    <!-- ── Central content block ── -->
    <LinearLayout
        android:id="@+id/contentBlock"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        app:layout_constraintBottom_toTopOf="@id/btnContact"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.36">

        <!-- Salmon card + support badge (overlapping) -->
        <FrameLayout
            android:layout_width="180dp"
            android:layout_height="190dp">

            <!-- Salmon rounded-square card with building icon -->
            <FrameLayout
                android:layout_width="170dp"
                android:layout_height="170dp"
                android:layout_gravity="top|start"
                android:background="@drawable/bg_register_icon_card"
                android:padding="30dp">

                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_register_building" />
            </FrameLayout>

            <!-- White support-badge overlapping bottom-right of card -->
            <FrameLayout
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:layout_gravity="bottom|end"
                android:background="@drawable/bg_register_support_badge"
                android:elevation="4dp"
                android:padding="12dp">

                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_register_support" />
            </FrameLayout>
        </FrameLayout>

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:fontFamily="sans-serif-black"
            android:text="@string/register_title"
            android:textColor="@color/login_text_primary"
            android:textSize="22sp" />

        <!-- Description -->
        <TextView
            android:id="@+id/tvRegisterDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:gravity="center"
            android:lineSpacingExtra="3dp"
            android:textColor="@color/login_text_secondary"
            android:textSize="13sp" />

    </LinearLayout>

    <!-- Contact button pinned near bottom -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnContact"
        android:layout_width="0dp"
        android:layout_height="54dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="48dp"
        android:backgroundTint="@color/login_primary"
        android:drawableStart="@drawable/ic_phone_outline"
        android:drawablePadding="8dp"
        android:fontFamily="sans-serif-medium"
        android:text="@string/register_contact_button"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        app:cornerRadius="27dp"
        app:elevation="0dp"
        app:iconGravity="textStart"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
